<!DOCTYPE html>
  <html>
    <head>
      <meta charset="UTF-8">
      <title>Student Courses</title>
    </head>
    <body>
      <h1>Student</h1>
      <div class="student">
        <h3>Name:<br>
        <%= @student.name %></h3><br>
        <h4>Grade:<br>
        <%= @student.grade %></h4>
      </div><br>
      <h1>Courses</h1>
      <% @courses.each do |course| %>
        <div class="course">
          <p>Name:<br>
          <%= course.name %></p><br>
          <p>Topic:<br>
          <%= course.topic %></p><br>
        </div><br>
      <% end %>
    </body>
  </html>
